{
  "name": "hls-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "clean-uploads": "rm -rf uploads/*",
    "clean-hls": "rm -rf hls/*",
    "clean": "npm run clean-uploads && npm run clean-hls",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.1",
    "multer": "^1.4.5-lts.1",
    "serve-index": "^1.9.1"
  }
}
